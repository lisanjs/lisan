// import { parse } from '../../../../src';

// Discuss if conditional group checks are important!
// A c function calls its parent conditional group key
describe('Given {keyC: { one: c("keyC", num)}}', () => {
  it.skip('should throw error', () => {
    // content
  });
});

// A c function calls its parent conditional group key
describe('Given {keyC1: { one: c("keyC2", num)}, keyC2: {one: c("keyC1", num)}}', () => {
  it.skip('should throw error', () => {
    // content
  });
});
